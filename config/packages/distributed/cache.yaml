framework:
    cache:
        prefix_seed: identity-link
        app: cache.adapter.redis
        default_redis_provider: '%env(REDIS_DSN)%'

        pools:
            rate_limiter_redis:
                adapter: cache.adapter.redis